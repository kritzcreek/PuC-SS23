def countdown(x : Integer) : Text =>
    if str_eq(int_to_string(x))("0") then
        print("BOOM!")
    else
        let printedInt = int_to_string(x) in
        let _ = print(printedInt) in
        countdown(x - 1)

countdown(3)